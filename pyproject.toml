[tool.ruff]
target-version = "py311"
line-length = 88
extend-exclude = [
  ".venv",
  "evaluation/results",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
pythonpath = ["."]
